syntax = "proto3";

package tradier_core;

option go_package = "./proto;tradier_core";

service Tradier_core {
	rpc Call(Request) returns (Response) {}
}

message Request {
	string name = 1;
}

message Response {
	string msg = 1;
}
